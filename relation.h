#pragma once
#include <stdlib.h>
#include <stdbool.h>
#include "utils.h"

// bool relation_is_reflexive(Relation* rel);
// bool relation_is_symmetric(Relation* rel);
// bool relation_is_antisymmetric(Relation* rel);
// bool relation_is_transitive(Relation* rel);
bool set_pair_contains(set_pair_int_t relation, pair_int_t pair);